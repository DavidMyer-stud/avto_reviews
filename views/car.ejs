<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title><%= car.brand %> <%= car.model %> - Відгуки</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5 mb-5">
    <a href="/" class="btn btn-outline-secondary mb-4">← На головну</a>

    <div class="card shadow-sm mb-5">
        <div class="row g-0">
            <div class="col-md-6">
                <% if (car.image_url) { %>
                    <img src="<%= car.image_url %>" class="img-fluid rounded-start" alt="<%= car.brand %>" style="width: 100%; height: 400px; object-fit: cover;">
                <% } else { %>
                    <div class="d-flex align-items-center justify-content-center bg-secondary text-white" style="height: 400px;">
                        <span>Фото відсутнє</span>
                    </div>
                <% } %>
            </div>
            
            <div class="col-md-6">
                <div class="card-body d-flex flex-column justify-content-center h-100 p-5">
                    <h1 class="display-4 fw-bold"><%= car.brand %> <%= car.model %></h1>
                    <p class="text-muted">ID автомобіля: <%= car.id %></p>
                    <p class="lead">Читайте реальні відгуки власників або залиште свій.</p>
                    
                    <a href="/review/<%= car.id %>" class="btn btn-primary btn-lg mt-3">✍️ Написати відгук</a>
                </div>
            </div>
        </div>
    </div>

    <h3 class="mb-4 border-bottom pb-2">Відгуки користувачів (<%= reviews.length %>)</h3>
    
    <% if (reviews.length === 0) { %>
        <div class="alert alert-info text-center">
            Поки немає відгуків про це авто. Будьте першим, хто напише правду!
        </div>
    <% } %>

    <div class="row">
        <% reviews.forEach(function(review) { %>
            <div class="col-12 mb-3">
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-white d-flex justify-content-between align-items-center">
    <div>
        <strong>👤 <%= review.login %></strong> 
        <span class="badge bg-secondary ms-2">Користувач</span>
    </div>
    
    <div>
        <small class="text-muted me-2">#<%= review.id %></small>
        
        <% if (user === 'admin') { %>
            <form action="/admin/delete-review/<%= review.id %>" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Точно видалити?');">🗑</button>
            </form>
        <% } %>
    </div>
</div>
                        <small class="text-muted">Відгук #<%= review.id %></small>
                    </div>
                    <div class="card-body">
                        <div class="row mb-3 text-center g-2">
                            <div class="col-sm-4">
                                <div class="p-2 border rounded bg-light">
                                    🤖 Двигун: <br>
                                    <b class="text-primary"><%= review.rating_engine %>/5</b>
                                </div>
                            </div>
                            <div class="col-sm-4">
                                <div class="p-2 border rounded bg-light">
                                    💺 Салон: <br>
                                    <b class="text-primary"><%= review.rating_interior %>/5</b>
                                </div>
                            </div>
                            <div class="col-sm-4">
                                <div class="p-2 border rounded bg-light">
                                    🛠 Надійність: <br>
                                    <b class="text-primary"><%= review.rating_reliability %>/5</b>
                                </div>
                            </div>
                        </div>
                        
                        <h6 class="card-subtitle mb-2 text-muted">Коментар:</h6>
                        <p class="card-text fs-5"><%= review.text %></p>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</div>

</body>
</html>