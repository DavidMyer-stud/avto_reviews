<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Мій профіль</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <div class="row">
        <div class="col-md-4 mb-4">
            <div class="card shadow text-center p-4">
                <div class="mb-3">
                    <span style="font-size: 50px;">👤</span>
                </div>
                <h3><%= user %></h3>
                <p class="text-muted">Користувач сайту</p>
                <hr>
                <a href="/" class="btn btn-outline-primary w-100 mb-2">На головну</a>
                <a href="/logout" class="btn btn-danger w-100">Вийти</a>
            </div>
        </div>

        <div class="col-md-8">
            <h2 class="mb-4">Мої відгуки (<%= reviews.length %>)</h2>

            <% if (reviews.length === 0) { %>
                <div class="alert alert-warning">
                    Ви ще не залишили жодного відгуку. <a href="/">Вибрати авто</a>
                </div>
            <% } %>

            <% reviews.forEach(function(review) { %>
                <div class="card shadow-sm mb-3">
                    <div class="card-header d-flex justify-content-between">
                        <strong>🚗 <%= review.brand %> <%= review.model %></strong>
                        <a href="/car/<%= review.real_car_id %>" class="btn btn-sm btn-primary">Перейти до авто</a>
                    </div>
                    <div class="card-body">
                        <div class="row text-center mb-2">
                            <div class="col">Двигун: <b><%= review.rating_engine %></b></div>
                            <div class="col">Салон: <b><%= review.rating_interior %></b></div>
                            <div class="col">Надійність: <b><%= review.rating_reliability %></b></div>
                        </div>
                        <p class="border-top pt-2"><%= review.text %></p>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</div>

</body>
</html>