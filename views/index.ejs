<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Всі авто</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>🚗 Рейтинг Автомобілів</h1>
        
        <% if (user) { %>
            <div>
    <a href="/profile" class="btn btn-outline-dark me-2">
        👤 <b><%= user %></b>
    </a>
    
    <% if (user === 'admin') { %>
        <a href="/admin" class="btn btn-warning btn-sm me-2">⚙️ Адмінка</a>
    <% } %>

    <a href="/logout" class="btn btn-danger btn-sm">Вийти</a>
</div>
        <% } else { %>
            <div>
                <a href="/login.html" class="btn btn-outline-primary">Увійти</a>
                <a href="/register.html" class="btn btn-primary">Реєстрація</a>
            </div>
        <% } %>
    </div>

<div class="card shadow-sm mb-4 p-3 bg-white">
        <form action="/" method="GET" class="row g-2">
            <div class="col-md-10">
                <input type="text" name="search" class="form-control" placeholder="🔍 Введіть марку або модель (наприклад: BMW)" value="<%= searchStr %>">
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary w-100">Знайти</button>
            </div>
        </form>
        <% if (searchStr) { %>
            <div class="mt-2">
                <a href="/" class="text-danger">❌ Скинути пошук</a>
            </div>
        <% } %>
    </div>

    <div class="row">
        <% cars.forEach(function(car) { %>
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm h-100">
                    
                    <% if (car.image_url) { %>
                        <img src="<%= car.image_url %>" class="card-img-top" alt="<%= car.model %>" style="height: 200px; object-fit: cover;">
                    <% } else { %>
                        <img src="https://via.placeholder.com/300x200?text=No+Image" class="card-img-top" style="height: 200px; object-fit: cover;">
                    <% } %>

                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title"><%= car.brand %> <%= car.model %></h5>
                        <p class="card-text text-muted flex-grow-1">ID авто: <%= car.id %></p>
                        
                        <div class="d-flex gap-2 mt-auto">
                            <a href="/car/<%= car.id %>" class="btn btn-outline-primary w-100">Читати відгуки</a>
                            <a href="/review/<%= car.id %>" class="btn btn-success w-100">+</a>
                        </div>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</div>

</body>
</html>